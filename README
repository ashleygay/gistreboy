This project requires boost and GTK3 to compile.

To compile : make

To change branches:
	git checkout <branch>

branches:
	master
	dev_processor
	dev_memory
	dev_integration

The list of instructions is in the file : instructionlist
